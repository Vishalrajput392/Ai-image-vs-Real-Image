# AI-Generated vs Real Image Detection — Dependencies
# NOTE: Image decode/preprocess uses PyTorch + OpenCV only. Pillow is
# intentionally avoided (project decision, see chat).

# --- Core deep learning ---
# Install the CUDA-matched build for your RTX 3060 separately, e.g.:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.2.0
torchvision>=0.17.0

# --- Image I/O / preprocessing (no Pillow) ---
opencv-python>=4.9.0

# --- Data handling ---
pandas>=2.0.0
pyarrow>=14.0.0        # needed to read the RealSS .parquet files
numpy>=1.26.0

# --- Splitting / metrics ---
scikit-learn>=1.4.0

# --- Utilities ---
tqdm>=4.66.0
pyyaml>=6.0

# --- Experiment tracking (SRS Sec 16) ---
mlflow>=2.10.0

# --- Backend / Frontend (SRS Sec 14) ---
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.9
streamlit>=1.32.0
requests>=2.31.0
